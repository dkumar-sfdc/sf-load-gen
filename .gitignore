# JMeter run artifacts
results/
*.jtl
*.log
jmeter.log

# Real credentials - commit only the per-scenario sample *_users.csv
user-files/*.csv
!user-files/*_users.csv
user-files/*.enc

# JWT private keys — never commit real keys; sample is tracked
user-files/*.pem
!user-files/jwt_key.example.pem

# OS
.DS_Store
